# Copy to .env and replace the <placeholder>. Only VITE_* vars reach the browser, so never put secrets here.
# Local backend: http://localhost:8001 (Docker) or http://localhost:8000 (uv)
VITE_API_BASE_URL=<your-backend-url>
